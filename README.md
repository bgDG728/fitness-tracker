# 健身飲食訓練系統

不只是紀錄工具,是會依照你的表現主動給建議的健身/飲食追蹤系統。用 Streamlit +
SQLite 打造,單機本地執行,個人健康資料不會上傳到任何地方。

## 功能

**個人設定**
- 身高/體重/年齡/性別/活動量/目標,用 Mifflin-St Jeor 公式算 BMR、TDEE
- 依目標(減脂/維持/增肌)自動算出每日熱量與三大營養素目標

**飲食紀錄**
- **食物資料庫搜尋**(Open Food Facts / Search-a-licious API,完全免費、不需要
  任何帳號或金鑰):輸入食物名稱自動帶出每 100g 熱量/營養素,填份量(g)自動
  換算,不用自己查營養標示、不用自己算
- 也可以手動新增餐點(熱量/蛋白質/碳水/脂肪),當日總計 vs 目標即時對比
- **飲食建議**:依今天營養素落差主動提示(例如蛋白質不足時建議食物方向、
  熱量過高或過低時提醒),不是只有冷冰冰的數字

**訓練紀錄**
- 記錄動作/組數/次數/重量
- **教練建議**:選動作時,依上次訓練表現用「雙重漸進」(double progression)
  邏輯建議這次的重量與次數區間,並附上建議理由
- **姿勢要點**:常見動作(深蹲/臥推/硬舉/肩推/划船/二頭彎舉等)的文字化姿勢提醒
- 各動作歷史最大重量(PR)一覽

**進度圖表**
- 體重趨勢、每日熱量攝取趨勢(含目標參考線)、訓練量趨勢、單一動作歷史重量趨勢

## 專案結構

```
fitness_tracker/
├── app.py                今日總覽首頁
├── db.py                 SQLite 資料庫層
├── calc.py                TDEE / 三大營養素計算
├── coach.py               教練建議邏輯(重量建議/姿勢要點/飲食建議)
├── pages/
│   ├── 1_個人設定.py
│   ├── 2_飲食紀錄.py
│   ├── 3_訓練紀錄.py
│   └── 4_進度圖表.py
├── fitness.db             SQLite 資料庫檔(不會被 git 追蹤,個人資料留在本機)
└── requirements.txt
```

## 使用方式

```
python -m venv venv
venv\Scripts\python.exe -m pip install -r requirements.txt
venv\Scripts\python.exe -m streamlit run app.py
```

打開 http://localhost:8502(或 Streamlit 顯示的網址),先到「個人設定」填一次
基本資料,才能算出熱量目標。之後每天用「飲食紀錄」「訓練紀錄」記錄,首頁跟
「進度圖表」會自動彙整。

## 教練建議的邏輯說明

**重量建議**用的是「雙重漸進」,重訓界常見、有實證基礎的簡單漸進超負荷方法:
- 設定次數目標區間(預設 6-10 下)
- 上次在這個重量做到區間上限(10 下)→ 這次加重(大肌群/複合動作 +2.5kg,
  小肌群/單關節動作 +1kg),次數會退回區間下限是正常現象
- 上次沒做到區間下限(6 下)→ 這次維持同重量,先把次數練起來
- 介於中間 → 同重量,目標多做 1 下

這是基於「最近一次該動作的最重那組」表現計算,不是什麼複雜的 AI 模型,邏輯
透明、你可以自己驗證是否合理。

## 目前沒做的部分(刻意先跳過)

- **拍照估算食物熱量**:需要串接視覺辨識 API(例如 Claude 的圖像辨識或專門的
  食物辨識服務),會產生實際 API 費用,需要申請並提供 API key。已改用免費的
  Open Food Facts 食物搜尋取代(見上方「食物資料庫搜尋」),沒有金鑰/費用門檻。
- **動作影片標準化分析**:這是姿態辨識(pose estimation)等級的電腦視覺專案,
  複雜度遠超過目前這個系統,先不做,避免做半套。
- **動作示範動畫/影片**:目前只做文字化的姿勢要點,沒有嵌入圖片或影片,
  避免有未經授權使用素材的疑慮。之後如果要加,會先找 MIT/CC 授權的開源動作
  資料庫(例如 free-exercise-db)確認授權條款,而不是隨便抓圖。

## 隱私

`fitness.db` 已加進 `.gitignore`,不會被推上 GitHub —— 這是你的體重、飲食、
訓練紀錄,屬於個人健康資料,只留在你自己的電腦上。如果之後想比照 podcast
聽力系統部署到雲端讓手機隨時能用,需要另外討論資料庫要放哪裡(因為雲端部署
的話,免費方案通常無法持久保存 SQLite 檔案,重新部署會遺失資料)。
